import React from 'react';
import MainPage from '../components/MainPage';

const MainPage = () => {
  // Logic for fetching blog posts from the server or state

  return (
    <MainPage />
  );
};

export default MainPage;
